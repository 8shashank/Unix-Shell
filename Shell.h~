#ifndef SHELL_H
#define SHELL_H
#include <unordered_map>

class Shell{
  
  Shell(std::string dir);

  void start();

 private:
  std::string currDir;
  std::unordered_map<int,Process> processMap;
  
}
#include "Shell.cpp"

#endif
